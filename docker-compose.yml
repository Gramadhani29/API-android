version: '3.8'

services:
  graphql-api:
    build: .
    container_name: buku-graphql-api
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
      - FLASK_APP=app.py
    restart: unless-stopped
    command: python app.py
